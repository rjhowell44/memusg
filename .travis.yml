language: python
python:
  - '3.5'
  
install:
- mkdir -p $HOME/bin
- curl -s https://testspace-client.s3.amazonaws.com/testspace-linux-dev.tgz | tar -zxvf- -C $HOME/bin
- testspace config url http://${TESTSPACE_ACCESS_TOKEN}:@open.stridespace.com
  
script: ./memusg -o sleep 2
